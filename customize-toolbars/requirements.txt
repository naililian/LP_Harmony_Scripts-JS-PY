# Customize Toolbars builder — Python 3.9+ (Harmony ships 3.9).
#   run.bat  does this for you, or:
#   py -3.9 -m venv .venv  &&  .venv\Scripts\python -m pip install -r requirements.txt

# The builder runs the real engine to preview registrations and name badge icons.
dukpy>=0.5

# Validates toolbar configs against the engine schema.
jsonschema>=4

# The desktop window.
PySide6>=6.5 ; python_version >= "3.9"
